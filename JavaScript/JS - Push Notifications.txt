
  # Codigo Utilitario para utilizar las Push Notifications

  if(window.Notification && Notification.permission !== "denied"){
    Notification.requestPermission(status => {
      let notification = new Notification('Orden de Compras', {
        body: 'Tecfenix, C.A. Ha generado su pedido',
        icon: 'http://gcamerida.fenixgold.com/logo.png'
      }
    }
  }

  # Ver mas documentacion acerca de la clase Notification()