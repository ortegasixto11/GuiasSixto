
// Las vistas en MySQL son tablas virtuales que nos ayudan a agilizar nuestro trabajo

// Crear una vista
  CREATE VIEW ${nombreVista} AS ${ComandoSelect};

  CREATE VIEW viewClientes AS SELECT id, name, role FROM clientes;

// Eliminar View
  DROP VIEW viewClientes; 