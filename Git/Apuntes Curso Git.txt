git init // para inicializar repositorios

git status // para ver los status

git add . // para anadir multiples archivos

git reset HEAD nombreArchivo // para sacar archivo del commit (CREO tengo que investigar) // estatus del noConfirmado

git commit -m "mensaje" // envio los archivos

git add y git commit  // son para enviar los archivos al repositorio

git checkout -- nombreArchivo  // para regresar a los datos anteriores antes de ccommit

//crear archivo .gitignore para ignorar archivos

git checkout nombreArchivo // para recuperar archivos borrados en mi workspace

git rm nombreArchivo // para borrar archivos de git

git log // para ver el historial de los commits

git checkout numeroDeCommit // para regresar en el tiempo

